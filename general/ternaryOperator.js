
function ageRestriction(age) {
    const minimumAge = 18;
    return (age >= minimumAge) 
    ? "You're elegible to access the website" 
    : "You aren't elegible to access the website";
};

// console.log(ageRestriction(15));
// console.log(ageRestriction(18));